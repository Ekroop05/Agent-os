# Agent OS

A starter workspace for coordinating planner, coder, and security agents through a React dashboard and FastAPI backend.

## Development

Run the backend:

```bash
cd backend
uvicorn app.main:app --reload
```

Run the frontend:

```bash
cd frontend
npm install
npm run dev
```
